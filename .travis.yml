language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
# command to install dependencies
install: 
  - pip install pybuilder
  - pyb install_dependencies
# command to run tests
script: 
  - pyb all_tests
env:
  - BOOKIT_USERNAME=username BOOKIT_PASSWORD=password
